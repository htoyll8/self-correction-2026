[project]
name = "mend"
version = "0.1.0"
description = "Framework for benchmarking LLM self-correction strategies"
requires-python = ">=3.10"
readme = "README.md"

[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"